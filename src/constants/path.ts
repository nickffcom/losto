const path = {
  home: '/',
  profile: '/profile',
  login: '/login',
  register: '/register',
  productlist: '/productlist',
  productDetail: '/:nameId',
  cart: '/cart',
  logout: '/logout'
} as const

export default path
