const path = {
  home: '/',
  profile: '/profile',
  login: '/login',
  register: '/register',
  productlist: '/productlist',
  productDetail: '/:id',
  logout: '/logout'
} as const

export default path
