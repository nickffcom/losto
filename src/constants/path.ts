const path = {
  home: '/',
  profile: '/profile',
  login: '/login',
  register: '/register',
  productlist: '/productlist',
  productDetail: '/:nameId',
  logout: '/logout'
} as const

export default path
